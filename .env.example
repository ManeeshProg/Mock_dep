# Backend API URL
# For localhost development
VITE_BACKEND_URL="http://localhost:8080"

# For production/AWS deployment, use your AWS backend URL with HTTPS
# VITE_BACKEND_URL=https://your-backend-url.com

# Optional: OpenAI API key for server-side Whisper transcription over WebSocket
# If you set this, the backend `/ws/stt` endpoint will forward audio to OpenAI
# and return transcriptions. Leave empty to disable server-side transcription.
# OPENAI_API_KEY=your_openai_api_key_here
